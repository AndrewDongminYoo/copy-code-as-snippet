# Prettier friendly markdownlint config (all formatting rules disabled)
extends: markdownlint/style/prettier

# Inline HTML Warning
MD033: false

# CHANGELOG.md reuses "Added"/"Changed"/"Fixed" under every version heading (Keep a Changelog).
MD024:
  siblings_only: true
